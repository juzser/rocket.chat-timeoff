import { BlockBuilder } from '@rocket.chat/apps-engine/definition/uikit';
import { lang } from '../lang/index';

export async function requestWfhForm({ block }: {
    block: BlockBuilder,
}): Promise<void> {

}
